# ASIN 站外推广侦察 · 5 分钟上手

> 给同事用的傻瓜版说明。**不需要装 Claude Code，不需要任何 API key**，只要 Node.js + Chrome。
> 输入一个 ASIN，输出这个产品在 Facebook / Pinterest / Instagram / Google 上的站外推广痕迹（折扣码、促销链接、红人测评、affiliate 链接），产出 Markdown 报告 + Excel 明细表。

---

## 一、准备（只做一次，约 3 分钟）

### 1. 装 Node.js

下载 LTS 版：https://nodejs.org → 一路下一步。
装完在 cmd 里输 `node -v`，能打印版本号（如 `v22.x.x`）就成功了。

### 2. 下载本工具

```powershell
git clone https://github.com/C3H3NS/asin-offsite-promo-scanner
cd asin-offsite-promo-scanner\scripts
```

没装 git 的话，直接在 GitHub 页面点 `Code → Download ZIP`，解压即可。

### 3. 装依赖

在 `scripts` 目录下双击 **`setup.bat`**（Mac/Linux 跑 `bash setup.sh`）。
它会自动执行 `npm install` 并下载 Playwright 浏览器内核。第一次会慢一点，耐心等。

---

## 二、每次使用（2 步）

### 第 1 步：启动调试 Chrome 并登录

双击 **`scripts\start_chrome_debug.bat`**

> ⚠️ 它会先关掉你所有的 Chrome 窗口，**记得先保存正在编辑的网页内容**。

弹出一个新的 Chrome 窗口后，在里面登录：

- **Facebook**（最重要，站外折扣码主要在这里）
- Pinterest、Instagram（可选，登录了覆盖更全）
- Google 保持已登录状态更稳

> 登录态会一直保留在这个 Chrome 里，下次不用重复登录，直接跑第 2 步就行。
> 只要不关掉这个窗口，可以连着扫很多个 ASIN。

### 第 2 步：跑扫描

**另开一个 cmd 窗口**，进到 `scripts` 目录，执行：

```powershell
scan.bat B0H6Q7VFK9
```

把 `B0H6Q7VFK9` 换成你要查的 ASIN 即可。就这一条命令，跑完全流程。

**运行时浏览器会自动移到屏幕外偷偷跑，不会弹到你面前打断工作**，跑完自动最小化到任务栏。

一个 ASIN 大约 2–5 分钟（取决于网络和渠道数量）。

---

## 三、报告在哪？

跑完终端最后会打印一屏，长这样：

```
════════════════════════════════════════════════════════
  ✅ 全部完成，报告在下面这个文件夹里：
  📁 C:\...\asin-offsite-promo-scanner\offsite-output

  ├─ report_Boytond.md     ← 看这个（Markdown 报告）
  ├─ findings_Boytond.csv   ← 明细表（双击用 Excel 打开）
  └─ scan_Boytond.json     ← 原始数据（给程序用）
════════════════════════════════════════════════════════
```

**报告默认在仓库里的 `offsite-output` 文件夹**（和 `scripts` 同级）：

| 文件 | 用途 | 怎么打开 |
|------|------|----------|
| `report_<品牌>.md` | **主要看这个**。人读报告，按渠道列出站外痕迹、折扣码、推广链接 | 记事本 / VSCode / Typora；也可拖进微信发给同事 |
| `findings_<品牌>.csv` | 明细表：ASIN / 渠道 / 类型 / URL / 标题 / 折扣码 / 折扣力度 / 备注 | **双击直接用 Excel 打开**，中文不乱码 |
| `scan_<品牌>.json` | 原始聚合数据 | 一般不用管 |
| `facebook_<品牌>.png` | FB 搜索结果截图，方便人工复核 | 双击看图 |

想换个地方存报告：

```powershell
scan.bat B0H6Q7VFK9 --out=D:\站外报告
scan.bat B0H6Q7VFK9 --out=%USERPROFILE%\Desktop\站外报告
```

---

## 四、常用参数

```powershell
:: 最常用：只给 ASIN，脚本自动抓亚马逊标题、解析品牌+产品词、动态拼查询（全自动，无需手传任何词）
scan.bat B0H6Q7VFK9

:: 极少数情况自动解析不准时，才用手动覆盖（一般不用加，见下方"重要提醒"）
scan.bat B0H6Q7VFK9 --brand=Boytond --product="AI Translation Earbuds"

:: 跳过某些渠道（比如 FB 登录不上就跳过）
scan.bat B0H6Q7VFK9 --skip-fb
scan.bat B0H6Q7VFK9 --skip-pinterest --skip-instagram

:: 换站点（默认美国站）
scan.bat B0H6Q7VFK9 --site=amazon.co.uk
scan.bat B0H6Q7VFK9 --site=amazon.de

:: 自定义报告目录
scan.bat B0H6Q7VFK9 --out=D:\站外报告

:: 想亲眼看浏览器怎么抓的（默认是静默的）
scan.bat B0H6Q7VFK9 --show

:: 连着扫多个 ASIN 时，跑完保持窗口隐藏，不要弹回来
scan.bat B0H6Q7VFK9 --keep-hidden
```

---

## 五、重要提醒

### ⚠️ 品牌 + 精确产品词脚本会自动解析，一般不用手传

脚本拿到 ASIN 后会自动抓亚马逊商品页：优先从 byline（"Visit the XXX Store"）解析品牌，
再从标题用通用算法提取精确产品词（如 `AI Translation Earbuds`），最后动态拼出查询。
**你只要甩一个 ASIN 即可，品牌和产品词都不用自己写。**

只有当自动解析明显不准（罕见，比如多词品牌被截断、或标题堆砌导致产品词跑偏）时，
才用 `--brand` / `--product` 手动覆盖：

```powershell
scan.bat B0H6Q7VFK9 --brand=Boytond --product="AI Translation Earbuds"
```

> 为什么产品词要"精确"？很多品牌下有多个型号（比如 Boytond 同时有 Q71 / Q16 / S19AI / S30i 好几款耳机）。
> 只搜品牌名会把同品牌其他型号的推广帖也捞回来，**折扣码对不上你要查的那个 ASIN**。
> 自动解析正是为了拿到这个精确产品词——所以首选"只传 ASIN"，让脚本自己提取。

报告里每条记录都有 `asin_match` 字段：

- `exact` = 确认是目标 ASIN 的推广 ✅ **重点看这些**
- `other` = 同品牌其他型号（参考价值有限）
- `unknown` = 帖子里没放 Amazon 链接，无法校验

### ⚠️ 只用那一个调试窗口

跑脚本期间，别再去双击普通的 Chrome 图标开新窗口，会抢占调试端口导致连不上。

---

## 六、出问题怎么办

| 现象 | 原因 | 解决 |
|------|------|------|
| `需要本机带调试端口的 Chrome` | 没跑第 1 步，或 Chrome 被关了 | 重新双击 `start_chrome_debug.bat` |
| `被踢到登录页` | 该平台登录态失效 | 在调试 Chrome 里重新登录那个平台，再跑一次 |
| 抓到的结果很少 | 页面没加载完 / 被降频 | 确认是用 `start_chrome_debug.bat` 启的 Chrome（内置了防降频参数），别自己手动开 |
| 9222 端口连不上 | 公司代理拦截了本地回环 | `start_chrome_debug.bat` 已内置 `--proxy-bypass-list`，如仍不行，临时关掉系统代理 |
| 亚马逊页面解析失败 | 触发了亚马逊反爬 | 在调试 Chrome 里手动打开一次商品页，过掉验证码，再重跑 |
| CSV 打开中文乱码 | 用了非 Excel 的工具 | 文件已带 BOM，Excel 双击正常；用记事本打开选 UTF-8 |

---

## 七、这个工具在查什么？

针对目标 ASIN，在下列渠道搜索并交叉验证：

| 渠道 | 找什么 |
|------|--------|
| **Facebook** | 折扣码群组、Deal 群发帖、红人种草帖（含 `l.facebook.com` 跳转链接自动解码还原真实 Amazon 链接） |
| **Pinterest** | 视觉种草 Pin、导购图文 |
| **Instagram** | 红人测评、`#AD` 标注的付费推广帖 |
| **Google** | `site:` 索引的站外测评、榜单、affiliate 联盟文章、折扣码站 |

每条结果都会尝试提取：**折扣码**、**折扣力度**、**促销价格**、**是否 `#AD` 付费推广**、**Amazon 链接与 ASIN 校验结果**。
